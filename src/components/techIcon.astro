---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div class="flex flex-col items-center justify-center">
  <img src={`/images/techIcons/${name}.png`} class="m-3 h-10 w-10" />
  <p>{name}</p>
</div>
